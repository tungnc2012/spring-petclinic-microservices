apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-config-server
  labels:
    {{- include "petclinic.labels" (dict "Release" .Release "Chart" .Chart "component" "config-server") | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.configServer.port }}
      targetPort: {{ .Values.configServer.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "petclinic.selectorLabels" (dict "Release" .Release "component" "config-server") | nindent 4 }}
